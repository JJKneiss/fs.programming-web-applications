import React from 'react';
// import './Header.css'

import Nav from '../Nav/Nav';

const Header = props => {
    return (
        <header>
            <h1>Hello World</h1>
            <Nav />
        </header>
    );
}

export default Header;